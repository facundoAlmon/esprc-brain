{
  "name": "esprc-brain-c6-webapp",
  "version": "1.0.0",
  "description": "Gulp build for ESP32 web interface",
  "scripts": {
    "build": "gulp build",
    "serve": "serve -s ./src -p 8080",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "del": "^6.0.0",
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-concat": "^2.6.1",
    "gulp-htmlmin": "^5.0.1",
    "gulp-inline-source": "^4.0.0",
    "gulp-terser": "^2.1.0"
  }
}
